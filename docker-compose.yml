services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: stargate
      POSTGRES_PASSWORD: stargate
      POSTGRES_DB: stargate
    ports:
      - "5432:5432"
    volumes:
      - stargate_pg:/var/lib/postgresql/data

volumes:
  stargate_pg:
